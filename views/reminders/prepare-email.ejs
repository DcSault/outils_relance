<%- include('../partials/header') %>

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <h2 class="card-title">
                            <i class="fas fa-envelope me-2 text-primary"></i>Préparer un Email
                        </h2>
                        <p class="card-text">Prévisualisez et envoyez un email pour la relance.</p>
                    </div>
                    <a href="/reminders" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i>Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-info-circle me-2"></i>Informations
                    </h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Matériel</span>
                            <span><%= item.name %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Destinataire</span>
                            <span><%= user.username %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Email</span>
                            <span><%= user.email || 'Non défini' %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Agence</span>
                            <span><%= agency ? agency.name : 'Non définie' %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Template</span>
                            <span><%= template.name %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold">Date de relance</span>
                            <span><%= new Date(reminder.reminderDate).toLocaleDateString('fr-FR') %></span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="col-md-8 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-envelope-open-text me-2"></i>Aperçu du mail
                    </h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Destinataire</label>
                        <input type="text" class="form-control" value="<%= user.email || 'Email non défini' %>" readonly>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Objet</label>
                        <input type="text" class="form-control" value="<%= mailInfo.subject %>" readonly>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Contenu</label>
                        <div class="card">
                            <div class="card-body bg-light">
                                <div class="email-preview">
                                    <% 
                                    // Afficher le contenu du mail avec les retours à la ligne
                                    // et en supprimant les espaces au début
                                    const formattedBody = mailInfo.body.trimStart().replace(/\n/g, '<br>');
                                    %>
                                    <%- formattedBody %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center">
                    <p class="mb-3">
                        <i class="fas fa-info-circle me-2 text-info"></i>
                        Cliquez sur le bouton ci-dessous pour ouvrir Outlook et envoyer le mail. Après l'envoi, confirmez que le mail a bien été envoyé.
                    </p>
                    <div class="d-flex justify-content-center gap-3">
                        <a href="<%= mailtoLink %>" class="btn btn-primary btn-lg" id="openOutlookBtn" target="_blank" rel="noopener noreferrer">
                            <i class="fas fa-envelope me-2"></i>Ouvrir dans Outlook
                        </a>
                        <a href="/reminders/confirm-sent/<%= reminder.id %>" class="btn btn-success btn-lg" id="confirmSentBtn">
                            <i class="fas fa-check-circle me-2"></i>Confirmer l'envoi
                        </a>
                        <a href="/reminders" class="btn btn-outline-secondary btn-lg">
                            <i class="fas fa-times-circle me-2"></i>Annuler
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .email-preview {
        font-family: Arial, sans-serif;
        color: #333;
        line-height: 1.6;
        white-space: pre-wrap;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Désactiver le bouton de confirmation jusqu'à ce que l'utilisateur ouvre Outlook
        const confirmSentBtn = document.getElementById('confirmSentBtn');
        const openOutlookBtn = document.getElementById('openOutlookBtn');
        
        confirmSentBtn.classList.add('disabled');
        confirmSentBtn.setAttribute('aria-disabled', 'true');
        
        // Activer le bouton de confirmation après avoir cliqué sur "Ouvrir dans Outlook"
        openOutlookBtn.addEventListener('click', function() {
            setTimeout(function() {
                confirmSentBtn.classList.remove('disabled');
                confirmSentBtn.removeAttribute('aria-disabled');
                
                // Afficher une notification
                const notification = document.createElement('div');
                notification.className = 'alert alert-info alert-dismissible fade show mt-3';
                notification.innerHTML = `
                    <i class="fas fa-info-circle me-2"></i>
                    Outlook a été ouvert. Une fois le mail envoyé, cliquez sur "Confirmer l'envoi".
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                `;
                
                document.querySelector('.card-body.text-center').appendChild(notification);
            }, 1000);
        });
    });
</script>

<%- include('../partials/footer') %> 